{
  "name": "upgrade-py-direct-reqs",
  "version": "0.5.0",
  "description": "A review & upgrade focused lightweight tool for upgrading direct Python dependencies in requirements.txt or pyproject.toml, with machine-readable JSON output.",
  "keywords": ["python", "dependencies", "CI", "automation", "AI", "upgrade"],
  "cli_commands": {
    "main": "upgrade-py-direct-reqs",
    "options": [
      {"plan ... --json --diff": "Enable machine-readable JSON output for AI parsing"},
      {"upgrade ... --yes": "upgrade packages in silent mode after policy review"}
    ]
  },
  "output_format": "JSON",
  "example_usage": "upgrade-py-direct-reqs plan --json --diff"
}